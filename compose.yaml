services:
  wordjumblebot:
    container_name: wordjumblebot
    image: git.postfmly.com/admin/wordjumblebot:arm64
    restart: unless-stopped
    ports:
      - "8004:8004"
    volumes:
      - db:/app/db
    environment:
      - LOGO_NAME=wordjumblebot.webp
      - LOGO_PATH=./utils/images
      - LOGO_PORT=8004
      - LOGO_URL=https://wordjumblebot.postfmly.com/wordjumblebot.webp
      - TZ=America/Chicago
    env_file: .env
    labels:
      - wud.display.name=WordJumbleBot
      - homepage.group=Personal
      - homepage.name=WordJumbleBot
      - homepage.icon=https://wordjumblebot.postfmly.com/wordjumblebot.webp

volumes:
  db:

networks: {}
